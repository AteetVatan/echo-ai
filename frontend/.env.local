# Server-side only — used by Next.js API route proxies and HMAC token signing.
# Never exposed to the browser (no NEXT_PUBLIC_ prefix).
ECHOAI_API_KEY=85785c95bf53a78ddfc42774d7f6cb572c0f217d9c6cd785abdbb49d6c7d8899

# Allowed origins for CSRF protection on API routes
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
